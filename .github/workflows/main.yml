name: OTE_Update

on:
  schedule:
    - cron: '55 * * * *' # Každou hodinu v 55. minutě
    # Extra aktualizace pro zachycení nových cen na další den:
    - cron: '45 11 * * *' # 13:30 UTC
    - cron: '45 12 * * *' # 14:30 UTC
    - cron: '45 13 * * *' # 14:30 UTC
    - cron: '45 14 * * *' # 14:30 UTC
    - cron: '45 15 * * *' # 14:30 UTC
  workflow_dispatch:      # Umožní spustit ručně tlačítkem

jobs:
  run_script:
    runs-on: windows-latest
    permissions:
      contents: write    # Důležité: umožní GitHubu zapisovat změny do CSV
    steps:
      - uses: actions/checkout@v4
      - name: Install ImportExcel
        shell: powershell
        run: Install-Module ImportExcel -Force -Scope CurrentUser
      - name: Run Script
        shell: powershell
        run: ./Get-OteCeny.ps1
